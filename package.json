{
  "name": "elm-switch",
  "version": "1.0.0",
  "description": "Elm Switch UI",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && elm make src/Main.elm --optimize --output=dist/elm.js && cp index.html dist && cp style.css dist && cp -r public dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MatsLanGoH/elm-switch.git"
  },
  "author": "Matthias Lambrecht",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/MatsLanGoH/elm-switch/issues"
  },
  "homepage": "https://github.com/MatsLanGoH/elm-switch#readme",
  "dependencies": {
    "elm": "0.19.1-3"
  },
  "devDependencies": {
    "elm-live": "^4.0.2"
  }
}
